{
  plugins = {
    mini = {
      enable = true;
      mockDevIcons = true;

      modules = {
        ai = { };
        align = { };
        basics = { };
        bracketed = { };
        git = { };
        icons = { };
        pairs = { };
      };
    };
  };
}
